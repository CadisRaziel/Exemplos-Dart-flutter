import 'package:flutter/material.dart';

class TelaSite extends StatefulWidget {
  @override
  _TelaSiteState createState() => _TelaSiteState();
}

class _TelaSiteState extends State<TelaSite> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Olá'),
      ),
    );
  }
}